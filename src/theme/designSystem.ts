// Minimal design tokens for layout rules
export const spacingTokens = {
  xs: 4,
  sm: 8,
  md: 16,
  lg: 24,
  xl: 32,
} as const;

export const shapeTokens = {
  corner: {
    small: 8,
    medium: 12,
    large: 16,
  },
} as const;
